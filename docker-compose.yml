version: "3.7"

services:
    nginx:
        build:
            context: ./nginx
        image: ${CURRENT_NGINX_IMG}
        ports:
            - 1935:1935
            - 8000:80
        networks:
            - bx-net
networks:
    bx-net:
        driver: bridge